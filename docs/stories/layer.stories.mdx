import { Story, Canvas, Meta } from "@storybook/addon-docs/blocks";
import { useEffect } from "@storybook/client-api";
import { viewinfo } from "../view/layer.view.js";

# 弹层（Layer）
<Meta title="复用组件/弹层(Layer)" />

<Canvas>
    <Story name="通用" height="150px" parameters={{ docs: { source: { code:`${viewinfo.message}${viewinfo.messageJS}` }}}}>
    {() => {
        useEffect(() => {
            Function(viewinfo.messageJS)()
        });
        return viewinfo.message
    }}  
    </Story>
</Canvas>

<Canvas>
    <Story name="iframe嵌套" height="150px" parameters={{ docs: { source: { code:`${viewinfo.ifm}${viewinfo.ifmJS}` }}}}>
    {() => {
        useEffect(() => {
            Function(viewinfo.ifmJS)()
        });
        return viewinfo.ifm
    }}  
    </Story>
</Canvas>

<Canvas>
    <Story name="可拖拽" height="150px" parameters={{ docs: { source: { code:`${viewinfo.drag}${viewinfo.dragJS}` }}}}>
    {() => {
        useEffect(() => {
            Function(viewinfo.dragJS)()
        });
        return viewinfo.drag
    }}  
    </Story>
</Canvas>