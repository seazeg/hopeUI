import { Story, Canvas, Meta } from "@storybook/addon-docs/blocks";
import { useEffect } from "@storybook/client-api";
import { viewinfo } from "../view/selector.view.js";

# 下拉框（Selector）
<Meta title="基础控件/下拉框(Selector)" />

<Canvas>
    <Story name="通用" height="300px" parameters={{ docs: { source: { code:`${viewinfo.general}${viewinfo.js}` }}}}>
    {() => {
        useEffect(() => {
            Function(viewinfo.generalJS)()
        });
        return viewinfo.general
    }}  
    </Story>
</Canvas>

<Canvas>
    <Story name="分组" height="300px" parameters={{ docs: { source: { code:`${viewinfo.group}${viewinfo.js}` }}}}>
    {() => {
        useEffect(() => {
            Function(viewinfo.groupJS)()
        });
        return viewinfo.group
    }}  
    </Story>
</Canvas>