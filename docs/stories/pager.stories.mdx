import { Story, Canvas, Meta } from "@storybook/addon-docs/blocks";
import { useEffect } from "@storybook/client-api";
import { viewinfo } from "../view/pager.view.js";

# 分页（Pager）
<Meta title="复用组件/分页(Pager)" />

<Canvas>
    <Story name="基础" height="150px" parameters={{ docs: { source: { code:`${viewinfo.general}${viewinfo.generalJS}` }}}}>
    {() => {
        useEffect(() => {
            Function(viewinfo.generalJS)()
        });
        return viewinfo.general
    }}  
    </Story>
</Canvas>

<Canvas>
    <Story name="扩展" height="150px" parameters={{ docs: { source: { code:`${viewinfo.extend}${viewinfo.extendJS}` }}}}>
    {() => {
        useEffect(() => {
            Function(viewinfo.extendJS)()
        });
        return viewinfo.extend
    }}  
    </Story>
</Canvas>
